{
    "name": "env-configurator",
    "version": "0.0.1",
    "description": "Configuration library that attempts to get service configuration based on DNS SRV records",
    "main": "index.js",
    "author": {
        "email": "mhughes@leisurelink.com",
        "name": "Michael Hughes"
    },
    "scripts": {
        "test": "mocha -R spec",
        "doc": "jsdoc -c ./jsdoc.conf -r -d ./documentation/"
    },
    "repository": {"type": "git", "url": "git+ssh://git@github.com:LeisureLink/env-configurator.git"},
  "private": true,
  "dependencies": {
    "assert-plus": "^0.1.5"
  },
  "devDependencies": {
    "expect": "^1.6.0",
    "mocha": "^2.1.0",
    "sandboxed-module": "^2.0.0",
    "sinon": "^1.12.2"
  }
}
